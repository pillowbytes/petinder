<div class="chat-container mt-3">
  <h2 class="mb-4">Mensagens de <%= @current_pet.name.capitalize %></h2>

  <div class="chat-list">
    <% if @matches.any? %>
      <% @matches.each do |match| %>
        <%= link_to pet_match_messages_path(match.pet, match), class: "match-link" do %>
          <div class="match-item animate__animated animate__fadeInUpBig ">
            <div class="match-avatar">
              <div class="split-avatar">
                <div class="half left" style="background-image: url(<%= cl_image_path(match.pet.photos.first.key) %>)"></div>
                <div class="half right" style="background-image: url(<%= cl_image_path(match.matched_pet.photos.first.key) %>)"></div>
              </div>
            </div>
            <div class="match-details">
              <strong><%= match.pet.name %> & <%= match.matched_pet.name %></strong>
                <% no_messages_phrases = [
                  "Nenhuma mensagem ainda. Dá um oi! 👋",
                  "Que tal quebrar o gelo com uma mensagem? ❄️",
                  "Comece a conversa! 🗨️",
                  "Não precisa ter vergonha, mande uma mensagem! 😊",
                  "Apresente seu pet com uma curiosidade divertida! 🐾",
                  "Esse chat tá quieto demais... bora animar! 🎤",
                  "Seu pet tá esperando por uma mensagem! 🐶",
                  "A primeira mensagem pode ser mágica! ✨",
                  "Puxe papo e faça um novo amigo! ❤️",
                  "Mande uma mensagem e veja onde vai dar! 🚀"
                ] %>
              <p><%= match.messages.any? ? match.messages.last.content : no_messages_phrases.sample %></p>
            </div>
          </div>
        <% end %>

      <% end %>

    <% else %>
      <p>Suas mensagens aparecerão aqui.</p>
    <% end %>
  </div>
</div>
